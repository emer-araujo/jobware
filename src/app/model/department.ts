// este será o model domain do projeto
// implementado com uma interface
export interface Department {
    // estabelecer o conjunto de regras pelas quais os dados serã manipulados dentro da aplicação
    name: string
    id: number
}
